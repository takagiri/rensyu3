import 'package:flutter/material.dart';

class Memory1 extends StatelessWidget {
  const Memory1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('メモリ1'),
      ),
    );
  }
}
