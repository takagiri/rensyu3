import 'package:flutter/material.dart';

class Memory2 extends StatelessWidget {
  const Memory2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('メモリ2'),
      ),
    );
  }
}
