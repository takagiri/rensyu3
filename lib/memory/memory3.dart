import 'package:flutter/material.dart';

class Memory3 extends StatelessWidget {
  const Memory3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('メモリ3'),
      ),
    );
  }
}
