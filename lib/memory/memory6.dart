import 'package:flutter/material.dart';

class Memory6 extends StatelessWidget {
  const Memory6({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('メモリ6'),
      ),
    );
  }
}
